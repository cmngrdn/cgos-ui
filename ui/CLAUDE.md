# Atom inventory — single source of truth

**Read [`docs/subsystems/atom-inventory.md`](../docs/subsystems/atom-inventory.md) before making a non-trivial change in this directory.** (62,815 bytes — the full architecture, conventions and the failures they came from.)

It is not in the root `CLAUDE.md` because that file is read in full at every session start in this repo, and this subsystem is only relevant when you are standing where you are now. This pointer is the trade: it costs nothing until you are here, and the content has one home rather than a copy per directory.

New durable lessons about this subsystem go in that file, or in `docs/patterns.md` if they are a record rather than a rule — not back into the root `CLAUDE.md`.
